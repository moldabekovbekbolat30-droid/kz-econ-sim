# Зависимости пайплайна прогноза (Вариант B — serverless, без бэкенда).
# Базовый kz_data_pipeline.py работает на чистой стандартной библиотеке;
# эти пакеты нужны только для шага прогноза BigQuery/TimesFM.
google-cloud-bigquery>=3.25.0
# Удобная выгрузка результатов в pandas (если понадобится в ingest.py):
db-dtypes>=1.2.0
